<h1>Recently active users...</h1>

  <% @users.each do |user| %>
    <%= user.first_name %> <%= user.last_name %>
    <%= link_to 'More info...', user_path(user)%><br>
    <%= image_tag user.image.url(:large) %><hr>
  <% end %>
